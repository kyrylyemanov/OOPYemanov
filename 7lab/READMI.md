![alt text](logo-lit.jpg)

# Лабораторна робота №7: Віртуальні середовища та пакетні менеджери в Python 🐍

**Студент:** "Єманов Кирил"
**Група:** КН-33

## 🎯 Мета роботи
Ознайомитися з принципами ізоляції проєктів у Python. Навчитися створювати віртуальні середовища та керувати залежностями за допомогою стандартного `venv`, а також сучасних інструментів `pipenv` та `poetry`. Навчитися працювати зі сторонніми API.

---

## 🛠️ Етап 1: Стандартний `venv` та `pip`
На цьому етапі було створено базове віртуальне середовище за допомогою вбудованого модуля `venv`.
- Встановлено бібліотеку `requests`.
- Створено та запущено скрипт для отримання даних про поточний сезон аніме (Jikan API).

*c:\Users\emano\Downloads\Вірутальна папка звіт.png*

---

## 📦 Етап 2: Менеджер пакетів `pipenv`
Робота з `pipenv` — інструментом, що об'єднує `Pipfile` та віртуальне середовище.
- Ініціалізовано середовище.
- Встановлено залежності.
- Скрипт успішно запущено через `pipenv run`.

*![alt text](<Вірутальна папка звіт-2.png>)*


---

## ✍️ Етап 3: Менеджер пакетів `poetry`
Робота з найсучаснішим інструментом для управління проєктами — `Poetry`.
- Налаштовано файл `pyproject.toml`.
- Встановлено бібліотеку `requests` через `poetry add`.

*![alt text](<якась поетри звіт.png>)*

---

## 🤖 Етап 4: Програма за допомогою ШІ (Завдання 7)
За допомогою ШІ (Gemini) було написано скрипт `app.py`, який звертається до відкритого API та виводить випадковий цікавий факт про котів. Запуск виконано ізольовано через Poetry.

*![alt text](<Звіт про котика.png>)*

---

## 🐉 Фінальне завдання (Допомога ШІ): Веб-сайт на Flask
Створено повноцінний локальний веб-сервер на базі мікрофреймворку Flask.
- Сервер звертається до Jikan API (сезонні аніме).
- Дані форматуються у вигляді HTML-сторінки та виводяться у браузер.
- Сервер запущено всередині середовища Poetry.

*![alt text](<Сайтт звіт.png>)*

---
**Висновок:** Під час виконання роботи було успішно протестовано 3 різні підходи до управління залежностями в Python. Отримано практичні навички роботи з API та підняття локального веб-сервера. Усі завдання виконані успішно.